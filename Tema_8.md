# Тема 8. Введение в ООП.
Отчет по Теме #8 выполнил:
- Куличихин Андрей Юрьевич
- ПИЭ-22-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Создайте класс “Car” с атрибутами производитель и модель. Создайте объект этого класса. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями.
```python
class Car: 
    def __init__(self, make, model):
        # Инициализируем атрибуты make (марка автомобиля) и model (модель автомобиля)
        self.make = make
        self.model = model

# Создаем экземпляр класса Car с маркой "Toyota" и моделью "Corolla"
my_car = Car("Toyota", "Corolla")
```
## Лабораторная работа №2
### Дополните код из первого задания, добавив в него атрибуты и методы класса, заставьте машину “поехать”. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.
```python
class Car:
    def __init__(self, make, model):
        # Инициализируем атрибуты make (марка автомобиля) и model (модель автомобиля)
        self.make = make
        self.model = model

    def drive(self):
        # Метод выводит сообщение о вождении автомобиля
        print(f"Driving the {self.make} {self.model}")

# Создаем экземпляр класса Car и вызываем метод drive
my_car = Car("Toyota", "Corolla")
my_car.drive()
```
### Результат.
![image](https://github.com/user-attachments/assets/af80e828-8003-4105-ac7e-350ca240e69b)
## Лабораторная работа №3
### Создайте новый класс “ElectricCar” с методом “charge” и атрибутом емкость батареи. Реализуйте его наследование от класса, созданного в первом задании. Заставьте машину поехать, а потом заряжаться. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.
```python
class Car:
    def __init__(self, make, model):
        # Инициализируем атрибуты make и model
        self.make = make
        self.model = model

    def drive(self):
        # Выводим сообщение о вождении автомобиля
        print(f"Driving the {self.make} {self.model}")

# Создаем экземпляр Car и вызываем метод drive
my_car = Car("Toyota", "Corolla")
my_car.drive()

# Класс ElectricCar является подклассом Car и добавляет новый атрибут - battery_capacity
class ElectricCar(Car):
    def __init__(self, make, model, battery_capacity):
        # Вызываем инициализацию родительского класса
        super().__init__(make, model)
        # Добавляем атрибут battery_capacity (емкость батареи)
        self.battery_capacity = battery_capacity

    def charge(self):
        # Метод выводит сообщение о зарядке электромобиля
        print(f"Charging the {self.make} {self.model} with {self.battery_capacity} kWh")

# Создаем экземпляр ElectricCar и вызываем методы drive и charge
my_electric_car = ElectricCar("Tesla", "Model S", 75)
my_electric_car.drive()
my_electric_car.charge()
```
### Результат.
![image](https://github.com/user-attachments/assets/7e290d05-5cfc-48fc-a2bf-3b08afe74d6f)
## Лабораторная работа №4
### Реализуйте инкапсуляцию для класса, созданного в первом задании. Создайте защищенный атрибут производителя и приватный атрибут модели. Вызовите защищенный атрибут и заставьте машину поехать. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.
```python
class Car:
    def __init__(self, make, model):
        self._make = make  # Атрибут с одним подчеркиванием указывает, что он защищенный
        self.__model = model  # Атрибут с двумя подчеркиваниями указывает на частный атрибут

    def drive(self):
        # Используем защищенные и приватные атрибуты в методе
        print(f"Driving the {self._make} {self.__model}")

# Создаем экземпляр класса Car
my_car = Car("Toyota", "Corolla")

# Обращаемся к защищенному атрибуту _make (возможно, но не рекомендуется)
print(my_car._make)

# Вызываем метод drive, который выводит марку и модель
my_car.drive()
```
### Результат.
![image](https://github.com/user-attachments/assets/40f65757-28c6-4fc9-85f0-12f306c5d971)
## Лабораторная работа №5
### Реализуйте полиморфизм создав основной (общий) класс “Shape”, а также еще два класса “Rectangle” и “Circle”. Внутри последних двух классов реализуйте методы для подсчета площади фигуры. После этого создайте массив с фигурами, поместите туда круг и прямоугольник, затем при помощи цикла выведите их площади. Напишите комментарии для кода, объясняющие его работу. Результатом выполнения задания будет листинг кода с комментариями и получившийся вывод в консоль.
```python
class Shape:
    # Метод area является абстрактным методом и будет переопределен в дочерних классах
    def area(self):
        pass

class Rectangle(Shape):
    def __init__(self, width, height):
        # Инициализируем атрибуты ширины и высоты прямоугольника
        self.width = width
        self.height = height

    def area(self):
        # Метод вычисляет и возвращает площадь прямоугольника
        return self.width * self.height

class Circle(Shape):
    def __init__(self, radius):
        # Инициализируем атрибут радиуса круга
        self.radius = radius

    def area(self):
        # Метод вычисляет и возвращает площадь круга
        return 3.14 * self.radius * self.radius

# Создаем список фигур с объектами Rectangle и Circle
shapes = [Rectangle(5, 4), Circle(3)]

# Вызываем метод area для каждой фигуры в списке и выводим результат
for shape in shapes:
    print(shape.area())
```
### Результат.
![image](https://github.com/user-attachments/assets/3d1928f4-3e2d-4d69-81ba-e183c582e050)
## Самостоятельная работа №1
### Самостоятельно создайте класс и его объект. Они должны отличаться, от тех, что указаны в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.
```python
class Book:
    def __init__(self, title, author):
        self.title = title
        self.author = author
novel = Book("1984", "Джорж Орвелл")
print(novel.title, novel.author)
```
### Результат.
![image](https://github.com/user-attachments/assets/cdb0d188-1d37-4640-8ac7-243eefc4e2d5)
## Выводы
Я узнал, как создать собственный класс, добавив к нему атрибуты для хранения информации, и как затем использовать объект этого класса для доступа к этим данным.
## Самостоятельная работа №2
### Самостоятельно создайте атрибуты и методы для ранее созданного класса. Они должны отличаться, от тех, что указаны в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.
```python
class Book:
    def __init__(self, title, author, year):
        self.title = title
        self.author = author
        self.year = year
    def summary(self):
        return f"'{self.title}' by {self.author}, published in {self.year}"
novel = Book("1984", "Джорж Орвелл", 1949)
print(novel.summary())
```
### Результат.
![image](https://github.com/user-attachments/assets/3b9bc3d4-1a7b-4df1-89c6-1a51c7abff12)
## Выводы
В процессе я научился добавлять новые атрибуты и методы к классу. Методы позволяют управлять поведением объекта, а атрибуты — хранить данные, относящиеся к этому объекту.
## Самостоятельная работа №3
### Самостоятельно реализуйте наследование, продолжая работать с ранее созданным классом. Оно должно отличаться, от того, что указано в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.
```python
class Book:
    def __init__(self, title, author, year):
        self.title = title
        self.author = author
        self.year = year
    def summary(self):
        return f"'{self.title}' by {self.author}, published in {self.year}"
class EBook(Book):
    def __init__(self, title, author, year, file_size):
        super().__init__(title, author, year)
        self.file_size = file_size
    def summary(self):
        return f"{super().summary()} (File size: {self.file_size}MB)"
ebook = EBook("1984", "Джорж Орвелл", 1949, 2)
print(ebook.summary())
```
### Результат.
![image](https://github.com/user-attachments/assets/2978f8ee-f462-4bbd-a29a-4919aeed3e57)
## Выводы
Я познакомился с наследованием, создав подкласс EBook, который расширяет класс Book. Это позволило мне переопределить метод summary и добавить новый атрибут file_size, что упростило создание объектов, обладающих схожими, но расширенными характеристиками.
## Самостоятельная работа №4
### Самостоятельно реализуйте инкапсуляцию, продолжая работать с ранее созданным классом. Она должна отличаться, от того, что указана в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.
```python
class Book:
    def __init__(self, title, author, year):
        self.__title = title
        self.__author = author
        self.__year = year
    def get_details(self):
        return f"'{self.__title}' by {self.__author}, published in {self.__year}"
novel = Book("1984", "Джорж Орвелл", 1949)
print(novel.get_details())
```
### Результат.
![image](https://github.com/user-attachments/assets/7ffde9b2-942c-46e5-9709-9c96c4b3de08)
## Выводы
Из этой задачи я понял, как использовать инкапсуляцию для защиты данных класса, делая атрибуты закрытыми. Это позволяет контролировать доступ к важным данным и обеспечивает их безопасность, добавляя гибкость для изменения методов работы с данными.
## Самостоятельная работа №5
### Самостоятельно реализуйте полиморфизм. Он должен отличаться, от того, что указан в теоретическом материале (методичке) и лабораторных заданиях. Результатом выполнения задания будет листинг кода и получившийся вывод консоли.
```python
class Book:
    def __init__(self, title):
        self.title = title
    def description(self):
        return "Физическая копия книги."
class EBook(Book):
    def description(self):
        return "Электронная копия книги."
novel = Book("1984")
ebook = EBook("1984")
print(novel.description())
print(ebook.description())
```
### Результат.
![image](https://github.com/user-attachments/assets/1bacffe5-37b8-4249-8c2a-7b5837e535fa)
## Выводы
Здесь я увидел, как полиморфизм позволяет создавать разные реализации методов для разных классов.
## Общие выводы по теме
Эти задачи помогли понять и применить ключевые принципы объектно-ориентированного программирования: создание классов, использование методов и атрибутов, наследование, инкапсуляция и полиморфизм. ООП делает код более гибким, масштабируемым и удобным для модификации.
